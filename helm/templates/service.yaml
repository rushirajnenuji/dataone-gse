apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataone-gse.fullname" . }}
  labels:
    {{- include "dataone-gse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dataone-gse.selectorLabels" . | nindent 4 }}
