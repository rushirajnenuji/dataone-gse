apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dataone-gse.fullname" . }}-test-connection"
  labels:
    {{- include "dataone-gse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dataone-gse.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
